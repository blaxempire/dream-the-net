import React from "react";
import { Button } from "react-magma-dom";

const Homeview = () => {
    return (
        <Button onClick={()=>{}} style={{backgroundColor: 'green'}}>

        </Button>
    )
}
export default Homeview
